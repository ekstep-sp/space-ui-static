/* tslint:disable:no-unused-variable */

import { TestBed, inject } from '@angular/core/testing'
import { BtnSocialLikeService } from './btn-social-like.service'

describe('Service: BtnSocialLike', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [BtnSocialLikeService],
    })
  })

  it('should ...', inject([BtnSocialLikeService], (service: BtnSocialLikeService) => {
    expect(service).toBeTruthy()
  }))
})
