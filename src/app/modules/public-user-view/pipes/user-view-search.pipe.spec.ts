import { UserViewSearchPipe } from './user-view-search.pipe';

describe('UserViewSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new UserViewSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
